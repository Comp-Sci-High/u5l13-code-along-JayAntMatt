<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Teachers</title>
  <link rel="stylesheet" href="/teachers.css" />
  <script src="/teacherScript.js" defer></script>
</head>
<body>
  <nav class="navbar">
    <div class="navbar-logo">
      <img src="https://images.squarespace-cdn.com/content/v1/5b328976aa49a1b7b46b89c3/1530039440176-O24B7OCR4WPIAI62JZTW/CompSci.High_Circle_Logo.png?format=750w" alt="School Logo" />
    </div>
    <div class="nav-links">
      <a href="/">Teachers</a>
      <a href="/ratings">Comments & Ratings</a>
    </div>
  </nav>

  <div class="container">
    <h1>Comp Sci High Teachers</h1>

    <div class="form-section">
      <h2>Add a New Teacher</h2>
      <!-- Create a form below that matches the Teacher schema -->
      <form>
        <label>Name: </label>
        <input type="text" name="name" required />

        <label>Department: </label>
        <input type="text" name="department" />

        <label>Image: </label>
        <input type="text" name="image" />

        <button type="submit">Submit</button>
      </form>
    </div>

    <div class="teachers-grid">
      <% for (let i = 0; i < teachers.length; i++) { %>
        <div class="teacher-card">
          <img src="<%= teachers[i].image %>" alt="<%= teachers[i].name %>" />
          <div class="teacher-card-content">
            <div class="edit-form" id="form-<%= teachers[i]._id %>">
              <label>Name: </label>
              <input type="text" class="teacher-name" value="<%= teachers[i].name %>" />
              
              <label>Department: </label>
              <input type="text" class="teacher-department" value="<%= teachers[i].department %>" />
              
              <label>Image: </label>
              <input type="text" class="teacher-image" value="<%= teachers[i].image %>" />
              
              <div class="button-group">
                <button type="button" onClick="updateTeacher('<%= teachers[i]._id %>')">Update Teacher</button>
                <button type="button" onClick="deleteTeacher('<%= teachers[i]._id %>')">Delete Teacher</button>
              </div>
            </div>
          </div>
        </div>
      <% } %>
    </div>

  </div>
</body>
</html>
